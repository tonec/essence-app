@AGENTS.md

# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

We're building the app described in @SPEC.md — a persistent multiplayer web app ("Star-Builder") where users buy coordinates on an infinite 2D galaxy map and build pixel art on 32×32 plots. Read `SPEC.md` for the data model, pricing tiers, API surface, and the reserve → Stripe checkout → webhook fulfillment flow.

The tree is early-stage: `src/app/` currently contains only the default `layout.tsx` / `page.tsx` / `globals.css`. Most of the architecture in `SPEC.md` is not yet implemented — build against the spec rather than looking for existing patterns to mirror.

Keep replies concise and focused on key information. No unnecessary fluff, no long code snippets.

## Commands

```bash
npm run dev        # next dev on http://localhost:3000
npm run build  # next build
npm run start      # next start
npm run lint       # eslint (flat config in eslint.config.mjs)
```

There is no test runner configured yet.

## Stack

- **Next.js 16** (App Router) with **React 19**, TypeScript strict mode. Path alias `@/*` → `src/*`.
- **Tailwind CSS v4** via `@tailwindcss/postcss` (no `tailwind.config` — v4 is CSS-first, configured in `src/app/globals.css`).
- Planned per SPEC: PixiJS (canvas), Supabase (Postgres + PostGIS), Redis (5-minute reservation locks), Stripe Checkout + webhooks, Cloudflare R2 (thumbnails). None are installed yet — add them as features land.

## Next.js 16 warning

Per `AGENTS.md`: this is Next.js 16, which has breaking changes from what's in the training data. Before writing route handlers, layouts, config, caching, or anything Next-specific, read the relevant guide in `node_modules/next/dist/docs/` and heed deprecation notices. The agent-rules block at the bottom of `README.md` is auto-regenerated by `next dev` — leave it committed rather than fighting it.

## Third-party libraries

When touching any third-party library, framework, SDK, or CLI, look up current docs via the **context7** MCP (`resolve-library-id` → `query-docs`) before writing code — training data may be stale. Prefer this over web search for library docs.

### Planned route structure

```
app/
  layout.tsx          # root layout (Geist fonts, TailwindCSS)
  page.tsx            # landing / redirect
  login/              # better-auth sign-in page
  register/           # better-auth sign-up page
  dashboard/          # star field page
  api/
    checkout/reserve  # Checks availability, sets 5-minute Redis lock, creates Stripe Session, returns checkout URL.
    webhooks/stripe   # Validates signature, verifies payment status
    map/chunks        # Array of occupied stars and thumbnail URLs within viewport bounds
    plots/:star_id    # Full `tile_data` JSON structure and metadata for editor loading
```
